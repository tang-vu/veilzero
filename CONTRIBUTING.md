# Contributing

Open an issue before large changes. Use conventional commits, strict TypeScript, bounded inputs and negative tests. Never commit credentials or case material. Every privacy claim must identify public and correlatable metadata. Mainnet writes require a browser-wallet human gate and must never use a raw key.

Run `pnpm check`, relevant browser tests, `scarb fmt --check`, `scarb build`, and `scarb test` before proposing a change.
